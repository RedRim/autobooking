# autobooking

Чтобы запустить проект, из корня выполните:

1) создать файл `.env` и перенести туда данные из `.env.example`
2) ввести команду `make up`

Приложение будет доступно на http://localhost:8000. API ключ задаётся в `.env` (по умолчанию из `.env.example` — `api_key_123`).
