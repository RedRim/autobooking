# Порт на хосте (внутри контейнера всегда 80)
FRONT_PORT=80

# Базовый URL API (в бандл на этапе build). Рекомендуется /api — nginx проксирует на бэк на этом же сервере.
# Локально: npm run dev + VITE_API_URL=/api и прокси в vite.config.js.
# VITE_API_URL=/api
